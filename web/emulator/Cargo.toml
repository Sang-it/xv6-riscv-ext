[package]
name = "xv6-web"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rvemu = { path = "vendor/rvemu" }

[profile.release]
opt-level = 3
lto = true
panic = "abort"
strip = true
